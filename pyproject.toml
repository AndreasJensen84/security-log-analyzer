[project]
name = "security-log-analyzer"
version = "0.1.0"
description = "A beginner-friendly CLI for analyzing sign-in logs."
requires-python = ">=3.13"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
